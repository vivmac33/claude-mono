# ═══════════════════════════════════════════════════════════════════════════
# MONOMORPH CI PIPELINE
# Runs on every PR and push to main
# ═══════════════════════════════════════════════════════════════════════════

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# Cancel in-progress runs for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20'

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # BUILD & VALIDATE
  # ─────────────────────────────────────────────────────────────────────────
  build:
    name: Build & Validate
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install root dependencies
        run: npm ci --ignore-scripts

      - name: 📦 Install web dependencies
        working-directory: apps/web
        run: npm install

      - name: 📦 Install server dependencies
        working-directory: apps/server
        run: npm install

      - name: 🔍 TypeScript Check (Web)
        working-directory: apps/web
        run: npx tsc --noEmit --skipLibCheck
        continue-on-error: true  # Don't fail build on TS errors for now

      - name: 🏗️ Build Web App
        working-directory: apps/web
        run: npm run build
        env:
          # Suppress Vite warnings in CI
          CI: true

      - name: 📊 Build Output Stats
        run: |
          echo "### 📦 Build Output" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -d "apps/web/dist" ]; then
            echo "**Web App Size:**" >> $GITHUB_STEP_SUMMARY
            du -sh apps/web/dist >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Asset Count:**" >> $GITHUB_STEP_SUMMARY
            find apps/web/dist -type f | wc -l >> $GITHUB_STEP_SUMMARY
          fi

      - name: 📤 Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: apps/web/dist
          retention-days: 7

  # ─────────────────────────────────────────────────────────────────────────
  # API HEALTH CHECK
  # ─────────────────────────────────────────────────────────────────────────
  health-check:
    name: API Health Check
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install server dependencies
        working-directory: apps/server
        run: npm install

      - name: 🚀 Start API Server
        working-directory: apps/server
        run: |
          node server.js &
          echo $! > server.pid
          sleep 3  # Wait for server to start

      - name: 🏥 Health Check
        run: |
          echo "### 🏥 Health Check Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Test health endpoint
          HEALTH_RESPONSE=$(curl -sf http://localhost:4000/api/v1/health)
          HEALTH_STATUS=$?
          
          if [ $HEALTH_STATUS -eq 0 ]; then
            echo "✅ **Health endpoint:** Passed" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            echo "$HEALTH_RESPONSE" | jq '.' >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Health endpoint:** Failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          # Test ready endpoint
          READY_RESPONSE=$(curl -sf http://localhost:4000/api/v1/ready)
          if [ $? -eq 0 ]; then
            echo "✅ **Ready endpoint:** Passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Ready endpoint:** Failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          # Test live endpoint
          LIVE_RESPONSE=$(curl -sf http://localhost:4000/api/v1/live)
          if [ $? -eq 0 ]; then
            echo "✅ **Live endpoint:** Passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Live endpoint:** Failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All health checks passed! 🎉" >> $GITHUB_STEP_SUMMARY

      - name: 🛑 Stop API Server
        if: always()
        working-directory: apps/server
        run: |
          if [ -f server.pid ]; then
            kill $(cat server.pid) 2>/dev/null || true
            rm server.pid
          fi

  # ─────────────────────────────────────────────────────────────────────────
  # LINT (Optional - Enable when ESLint is configured)
  # ─────────────────────────────────────────────────────────────────────────
  # lint:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run lint

  # ─────────────────────────────────────────────────────────────────────────
  # TEST (Optional - Enable when tests are added)
  # ─────────────────────────────────────────────────────────────────────────
  # test:
  #   name: Test
  #   runs-on: ubuntu-latest
  #   needs: [build]
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm test

  # ─────────────────────────────────────────────────────────────────────────
  # CI SUCCESS GATE
  # ─────────────────────────────────────────────────────────────────────────
  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [build, health-check]
    if: always()
    steps:
      - name: Check all jobs passed
        run: |
          if [ "${{ needs.build.result }}" != "success" ]; then
            echo "❌ Build job failed"
            exit 1
          fi
          if [ "${{ needs.health-check.result }}" != "success" ]; then
            echo "❌ Health check job failed"
            exit 1
          fi
          echo "✅ All CI checks passed!"
